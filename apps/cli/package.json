{
  "name": "@openpool/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "openpool": "bin/openpool.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@openpool/sdk": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitest": "^4.1.11"
  }
}
