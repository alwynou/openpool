{
  "name": "@openpool/migrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "openpool-migrator": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
