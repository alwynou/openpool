# ADR 0001：控制面与数据面分离

- 状态：Accepted
- 日期：2026-09-01

## 决策

Worker 负责认证、元数据、放置和签名；对象上传与下载通过签名 URL 在客户端和 Provider 之间
直接发生。

## 原因

这样可以避免 Worker 承担大对象内存、CPU、请求时长和带宽压力，也使存储容量与控制面规模
解耦。

## 后果

上传需要 reserve/complete 两阶段流程、过期会话清理和幂等处理。Provider 必须支持签名操作；
不具备该能力的 Provider 不能作为 V1 标准数据面。
